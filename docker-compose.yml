services:
  server:
    image: dezhishen/now-and-again
    ports:
      - "8080:8080"
    environment:
      - DATA_DIR=/data
      - GIN_MODE=release
      # JWT_SECRET=    # 可选，不设置则自动生成并持久化到 /data/.jwt_secret
    volumes:
      - data:/data
    restart: unless-stopped

volumes:
  data:
